-- <del>反正是自己糊的SQL方言 遇到难以实现的地方就瞎搞</del>

-- 首先写 scanner 扫一遍

需要处理以下: (相应地需要关心SQL的数据类型有哪些)

注释 
#后跟一行注释
-- 后面可以(最好)有一个空白符？
/**/ 注释

关键字

非关键字的 id  模仿C

numeric 类:
整数 bit tinyint smallint mediumint int bigint integer(=int)
bool: true(1) false(0)

修饰符: unsigned zerofill width(M)
cast as
浮点
decimal dec numeric fixed 

float

double double precision

日期时间类：
date datetime timestamp time year

String 类:

char varchar binary varbinary blob tinyblob mediumblob longblob text tinytext longtext 

enum set  

修饰符: character set, collate, national, width(M)

其它符号:

! " # $ % & ' ( ) * + , - . / : ; < = > ? @ [ \ ] ^ _ ` { | } ~


create database

create table

select

insert into

update set

delete from

join